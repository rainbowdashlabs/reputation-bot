# Backend Configuration
# Set these variables to configure the backend host and port
# If not set, the frontend will use relative URLs (same host as frontend)

# Backend host (can include protocol like http:// or https://, or just hostname/IP)
# Examples:
#   VITE_BACKEND_HOST=localhost
#   VITE_BACKEND_HOST=http://localhost
#   VITE_BACKEND_HOST=https://api.example.com
#   VITE_BACKEND_HOST=192.168.1.100
VITE_BACKEND_HOST=

# Backend port (optional, only needed if backend runs on non-standard port)
# Example:
#   VITE_BACKEND_PORT=8080
VITE_BACKEND_PORT=
